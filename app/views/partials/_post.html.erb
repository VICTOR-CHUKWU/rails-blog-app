<section class="row">
<div class="col-12 border">
      <p><strong>Post #<%= post_counter + 1 %></strong></p>
      <h3><%= post.title %></h3>
      <p> <%= post.text %> </p>
</div>
  <% end %>
  <div class="co-12 border">
    <%= render post.five_recent_comments %>
  </div>
</section>